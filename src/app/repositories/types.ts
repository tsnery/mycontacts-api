export type TContacts = {
  id: string
  name: string
  email: string
  phone: string
  category_id: string
}

export type TCreateCategoryProps = {
  name: string
}

export type TCategory = {
  id: string
  name: string
}
